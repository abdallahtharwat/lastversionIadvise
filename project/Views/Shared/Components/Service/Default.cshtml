@model IEnumerable<IconService>



<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle text-black" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        Services
    </a>
     <ul class="dropdown-menu">

        @foreach(var item in Model)
        {
            <li class="nav-item">
                <a class="dropdown-item " asp-area="Customer" asp-controller="ServicePage" asp-action="Details" asp-route-id="@item.Id">
                    @item.HomePageTitle
                </a>
            </li>
        }


     </ul>
</li>